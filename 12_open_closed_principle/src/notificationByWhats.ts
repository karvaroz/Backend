import NotificationCenter from './notification-center';
import User from './user';

export default class NotificationByWhats extends NotificationCenter {
    notifyByWhats(user: User, message: string){     
    }
}